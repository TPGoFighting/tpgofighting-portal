# Final motion route

The final route is the page-owned procedural vector implementation:

- markup and accessible labels: `../../../../index.html`
- scroll scrub controller: `../../../../app.js`
- responsive vector scenes: `../../../../style.css`
- timing and budget facts: `../build/timeline.json` and `../build/motion-budget.json`

No raster/video payload is required. Desktop keeps the vector scene at full fidelity; the mobile media query removes secondary decorative layers.
