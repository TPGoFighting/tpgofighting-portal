# Pilot status

The pilot uses the same procedural vector route as the delivered scenes. The page mount and first scroll segment are represented by `prompt-library` in `index.html`; no external video generation was run because the Oil Motion ZenMux key is not configured on this machine.

The deterministic page runtime is validated with the shared 60 fps scrub budget and reduced-motion fallback.
